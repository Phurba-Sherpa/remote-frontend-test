import styled from "styled-components"

const StyledLocationMap = styled.div`
    min-height: 100dvh;
`

export default function LocationMap() {
  return (
    <StyledLocationMap>LocationMap</StyledLocationMap>
  )
}
